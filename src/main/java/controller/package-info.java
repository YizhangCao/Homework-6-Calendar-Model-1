/**
 * Contains controller classes for the calendar application.
 * 
 * <p>This package provides the main application controller:
 * <ul>
 *   <li>{@link controller.CalendarController} - Main controller with application entry point</li>
 * </ul>
 * 
 * <p>The controller coordinates between the model and view layers,
 * handling calendar persistence and view management.
 */
package controller;
