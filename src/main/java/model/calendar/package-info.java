/**
 * Provides the core calendar management functionality.
 *
 * <p>This package contains the {@link model.calendar.Calendar} interface and its
 * implementation {@link model.calendar.CalendarImpl}, which manage events and
 * handle conflict detection based on configurable policies.
 *
 * @since 1.0
 */
package model.calendar;